const accountId = "roh334" 
// the constvariable can't change
let accpuntEmail = "rohit@google.com"
 // it can be change

var accountPassword = "334422"
 // var not use
accountCity = "guwahati" 
//javascript it assign as variable

console.table([typeof accountId,typeof accountPassword,typeof accpuntEmail,typeof accountCity]);